## Setup

Install the Rust toolchain (stable). After checking out the repository, run:

```bash
cd rust
cargo build
```

## Running tests

```bash
cd rust
cargo check
cargo test
```

Keep tests offline-friendly and deterministic.
